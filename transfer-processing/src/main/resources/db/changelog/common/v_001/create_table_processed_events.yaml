databaseChangeLog:
  - changeSet:
      id: create_table_processed_events
      author: DinaZ
      changes:
        - sql:
            sql: |
              CREATE TABLE IF NOT EXISTS processed_events (
              event_id VARCHAR(64) PRIMARY KEY,
              consumer_name VARCHAR(64) NOT NULL,
              processed_at TIMESTAMP NOT NULL
              );
